api:
  dashboard: true
  insecure: true

entrypoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

log:
  level: DEBUG

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
